{
  "name": "@assess/core",
  "version": "2.0.0",
  "type": "module",
  "description": "Graph contracts, validators, and typed reference helpers for assess. The executable runtime pipeline is packages/core/runtime/engine.mjs.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
